import {} from '../actions/actionTypes'

const initialState = {

}

export function reducer(state = initialState, {type, payload }){

    switch (type) {
        // case value:
            
        //     break;
    
        default:
            break;
    }

}